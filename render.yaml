services:
  - type: web
    name: jobpulse-backend
    env: docker
    region: oregon
    plan: free
    rootDir: backend
    envVars:
      - key: HEADLESS
        value: true
      - key: PORT
        value: 10000
      - key: DEEPSEEK_API_KEY
        sync: false
